# For PDF to Word conversion
pypdf2==3.0.1          # For handling PDF files (optional, depending on your needs)
pdfplumber==0.9.0      # For extracting text and images from PDFs
pdf2docx==0.5.6        # For converting PDF to Word

# For Word to PDF conversion
python-docx==0.8.11    # For handling .docx files
reportlab==3.6.13      # For generating PDF files

# Utility libraries
tkinter==0.1.0         # For building a graphical user interface (if needed)
PyQt5==5.15.7          # Optional: For a more sophisticated GUI with PyQt
watchdog==2.1.9        # Optional: For monitoring file system changes
